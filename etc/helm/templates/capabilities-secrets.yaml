apiVersion: v1
kind: Secret
metadata:
  name: site-capabilities-values
  namespace: {{ .Release.Namespace }}
data:
  iam_client_secret: {{ .Values.secrets.credentials.iam_client_secret }}
  mongo_password:  {{ .Values.secrets.credentials.mongo_password }}