import requests

url = "{{ api_server_url }}/services"

querystring = {"only_node_names":"SOME_STRING_VALUE","only_site_names":"SOME_STRING_VALUE","only_service_types":"SOME_STRING_VALUE","only_service_scope":"SOME_STRING_VALUE","include_inactive":"SOME_BOOLEAN_VALUE"}

headers = {"Authorization": "Bearer REPLACE_BEARER_TOKEN"}

response = requests.request("GET", url, headers=headers, params=querystring)

print(response.text)