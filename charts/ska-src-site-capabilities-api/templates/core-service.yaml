apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: {{ .Release.Namespace }}
  labels:
    app: core
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: core
  type: LoadBalancer